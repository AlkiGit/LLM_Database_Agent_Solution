# --- 1. Python 필수 제외 항목 ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# --- 2. 가상환경 (Virtual Environments) ---
# 가상환경 폴더명이 venv, .venv, env 중 무엇이든 커버
venv/
.venv/
env/
.env/
ENV/

# --- 3. 환경 변수 및 비밀 키 (가장 중요!) ---
# 실제 API Key가 들어있는 파일은 절대 커밋 금지
.env
.env.local
.env.*.local
*.key
*.pem

# (주의) 협업용 템플릿 파일은 제외하지 않음 (필요 시 주석 해제하여 사용)
# !.env.example
# !.env.template

# --- 4. IDE 및 에디터 설정 파일 ---
# VS Code
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# PyCharm (JetBrains)
.idea/

# Mac OS
.DS_Store

# --- 5. 데이터베이스 (로컬 테스트용) ---
# SQLite 등을 로컬에서 쓸 경우 제외
*.sqlite
*.sqlite3
*.db

# SQLAlchemy 마이그레이션 캐시 (alembic 사용 시)
__pycache__/

# --- 6. 로그 및 출력물 ---
# utils/logger.py 에서 생성하는 로그 파일들
*.log
logs/
pipeline_logs/

# --- 7. 테스트 및 커버리지 ---
.coverage
htmlcov/
.pytest_cache/

# --- 8. LangGraph / LLM Cache ---
# 로컬에 저장되는 체크포인트나 캐시 데이터
.langchain/
langgraph_checkpoints/